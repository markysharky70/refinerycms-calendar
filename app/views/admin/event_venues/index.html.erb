<div id='records'>
  <% if searching? %>
    <h2><%= t('results_for', :scope => 'shared.admin.search', :query => params[:search]) %></h2>
  <% end %>
  <% if @event_venues.any? %>
    <div class='pagination_container'>
      <%= render :partial => 'event_venues' %>
    </div>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'shared.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
<div id='actions'>
  <ul>
    <% if Admin::EventVenuesController.searchable? %>
      <li>
        <%= render :partial => "/shared/admin/search",
                    :locals => {
                      :url => admin_event_venues_url
                    } %>
      </li>
    <% end %>
    <li>
      <%= link_to t('.create_new'), new_admin_event_venue_url,
                   :class => "add_icon" %>
    </li>
    <li>
      <%= link_to t('.manage_events'), admin_events_url,
                   :class => "edit_icon" %>
    </li>

    <li>
      <%= link_to t('.manage_event_categories'), admin_event_categories_url,
                   :class => "edit_icon" %>
    </li>
  </ul>
</div>